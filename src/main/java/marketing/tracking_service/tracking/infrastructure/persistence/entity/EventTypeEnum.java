package marketing.tracking_service.tracking.infrastructure.persistence.entity;

public enum EventTypeEnum {
    PAGE_VIEW, VIEW_CONTENT, SCROLL, CLICK,
    FORM_START, FORM_SUBMIT, LEAD_SUBMIT, SIGNUP,
    DOWNLOAD, VIDEO_PLAY, VIDEO_COMPLETE, CUSTOM
}
